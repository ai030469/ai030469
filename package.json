{
  "name": "weekly-events-planner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "cd backend && npm install && node src/seed.js && cd ../frontend && npm install",
    "start": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "seed": "cd backend && node src/seed.js"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
